variables:
  $CI_REGISTRY:$CI_PIPELINE_IID

stages:
  - build

build:
  stage: build
  script:
    - 
    - 'docker build -t $CI_REGISTRY:$CI_PIPELINE_IID -t $CI_REGISTRY:latest .'
    - 'docker push $CI_REGISTRY'
